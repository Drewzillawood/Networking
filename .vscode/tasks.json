{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build programs",
            "type": "shell",
            "command": "${workspaceRoot}/lab\\ 9/makefile",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}